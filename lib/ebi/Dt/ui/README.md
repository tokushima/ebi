# DevTools UI

Developer Tools の React UI コンポーネント。

## 必要環境

- Node.js 18+
- npm

## ビルド方法

### npm を直接使う場合

```bash
cd lib/ebi/Dt/ui

# 依存関係のインストール
npm install

# 本番ビルド
npm run build
```

ビルドが完了すると `../assets/app.js` が生成されます。

